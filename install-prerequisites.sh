echo "Starting prerequisites installation script run" && sudo apt-get update && curl -sL https://deb.nodesource.com/setup_14.x | sudo -E bash - && sudo apt-get install -y nodejs && sudo npm install -g pm2 && sudo pm2 save && echo "End of script run"
